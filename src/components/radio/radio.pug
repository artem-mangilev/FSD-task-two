mixin radio(name, buttons, checked)
  .radio
    each button in buttons
      - const { value, text } = button
      label.radio__button
        input.radio__input(type='radio' name=name checked=(value === checked) value=value)
        .radio__text= text